# Windows: prefer install.bat (Python 3.10/3.11, then Coqui TTS + pinned transformers).
# If you install manually: default `pip install torch` is often CPU-only; for NVIDIA GPU
# use the wheel index from https://pytorch.org/get-started/locally/ matching your CUDA version.
customtkinter
tkinterdnd2
openai-whisper
# Coqui TTS pulls in `gruut`, which currently pins compatible ranges for these deps.
# Without pins, a later `pip install torch` can upgrade numpy/networkx too far and break TTS at runtime.
numpy>=1.22,<2
networkx>=2.5,<3
torch
deep-translator
TTS
transformers==4.39.3
huggingface_hub>=0.20.0
